<!--
テンプレート: スラッシュコマンド
使い方: {{...}} を置換。配置: .claude/commands/{{command-slug}}.md → /{{command-slug}} で起動
本文末の $ARGUMENTS にユーザー入力が差し込まれる。
このコメントは生成物には残さず削除する。
-->
---
description: {{このコマンドが何をするか1行}}
---

{{このコマンドの目的・切り替える業務}}を扱います。

## 手順
1. {{作業ディレクトリ / 起動するペルソナ}}
2. {{読み込むルール・CLAUDE.md}}
3. {{ユーザー依頼を該当作業にマッピング}}
   - 例: 「{{依頼例}}」→ {{動作}}

## ユーザー入力
$ARGUMENTS

上記入力をもとに、まず**何の作業か**を1行で宣言し、必要なら不足情報を確認してから着手してください。
